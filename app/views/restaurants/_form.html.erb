
<%= form_for @restaurant, :html => { :multipart => true } do |f| %>
	<%= f.file_field :image %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.text_field :description %>
	<%= f.submit %>
<% end %>